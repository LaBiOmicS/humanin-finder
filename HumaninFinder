#!/usr/bin/env python
import sys
import os

# Add src to path for direct execution from root
project_root = os.path.dirname(os.path.abspath(__file__))
sys.path.append(os.path.join(project_root, 'src'))

from humaninfinder.cli import main

if __name__ == "__main__":
    # If no subcommand is provided, default to predict (optional, but standard in Click is to show help)
    # To mimic metaLncRNA, we keep it as a group
    main()
